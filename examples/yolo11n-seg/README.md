# YOLO11n-seg demo

This demo follows the same command style as detection demos:

```text
image -> preprocess -> Neuron RuntimeV2 -> 10 raw outputs -> decode + NMS + mask proto compose -> result image/json
```

## Models

- `models/yolo11n-seg/int8/`
  - `yolo11n-seg_int8.dla`
  - `yolo11n-seg_mtk_int8.tflite`
- `models/yolo11n-seg/fp32/`
  - `yolo11n-seg_fp32.dla`
  - `yolo11n-seg_mtk_fp32.tflite`

## Build

```bash
cmake -S . -B build
cmake --build build -j
```

## Run

Run INT8 model:

```bash
./build/yolo11n-seg_demo
```

Run FP32 model:

```bash
./build/yolo11n-seg_demo --fp32
```

Run with specific images:

```bash
./build/yolo11n-seg_demo \
  --image assets/images/bus.jpg \
  --image assets/images/zidane.jpg
```

Run without `NeuronRuntimeV2_run` (still loads the `.dla`, prints each input/output buffer size in bytes, letterbox-preprocesses images, and checks the buffer matches `input[0]`):

```bash
./build/yolo11n-seg_demo --skip-runtime
```

## Generated Artifacts

- `outputs/yolo11n-seg/detections/<image>.json`
- `outputs/yolo11n-seg/vis/<image>_seg.png`

## Implementation Notes

- Input is `1x3x512x512` NCHW.
- Ten outputs are grouped as:
  - mask coeff heads: `1x32x64x64`, `1x32x32x32`, `1x32x16x16`
  - cls logits heads: `1x80x64x64`, `1x80x32x32`, `1x80x16x16`
  - bbox DFL heads: `1x64x64x64`, `1x64x32x32`, `1x64x16x16`
  - proto: `1x32x128x128`
- Postprocess does DFL decode (`reg_max=16`), class sigmoid, confidence filter, class-wise NMS,
  then keeps matched 32-dim mask coefficients for each kept detection.
- Instance mask is generated by `coeff @ proto`, then sigmoid + bbox crop + inverse letterbox mapping
  to original image size.
- In INT8 mode, each output tensor is dequantized to float before DFL, sigmoid, NMS and mask compose.
